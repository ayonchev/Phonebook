<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Phonebook.Views.ContactCreateEditPage"
             Padding="30">
     
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Cancel" Command="{Binding CancelCommand}" IconImageSource="close.png" />
        <ToolbarItem Text="Save" Command="{Binding SaveCommand}" IconImageSource="check.png" />
    </ContentPage.ToolbarItems>
    
    <StackLayout VerticalOptions="CenterAndExpand">
        <!--<Button Text="Choose a picture" Command="" />
        <Image x:Name="selectedImage" />-->
        <Label Text="Name" />
        <Entry Text="{Binding Contact.Name, Mode=TwoWay}" />
        <Label Text="Phone number" />
        <Entry Text="{Binding Contact.PhoneNumber, Mode=TwoWay}" />
        <Label Text="Description" />
        <Editor Text="{Binding Contact.Description, Mode=TwoWay}" AutoSize="TextChanges" />
        <Label Text="Category" />
        <Picker 
            Title="Choose a category" 
            ItemsSource="{Binding Categories}" 
            ItemDisplayBinding="{Binding Name}" 
            SelectedItem="{Binding SelectedCategory, Mode=TwoWay}" />
    </StackLayout>
</ContentPage>